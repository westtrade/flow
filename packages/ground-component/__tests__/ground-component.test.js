'use strict';

const groundComponent = require('..');
const assert = require('assert').strict;

assert.strictEqual(groundComponent(), 'Hello from groundComponent');
console.info('groundComponent tests passed');
